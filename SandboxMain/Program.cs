namespace Sandbox
{
    class Program
    {
        public static void Main(string[] args)
        {
            /* Empty entry point. This projects acts as a sandbox for different functionality related to the C# language,
             and tests these implementations via the SandboxUnitTests project */
        }
    }

}